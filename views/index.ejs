<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SpotiNode</title>
    <!-- Link to Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Reset some basic styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif; /* Updated font-family */
            background-color: #121212;
            color: #fff;
            padding: 20px;
        }

        h1 {
            color: #1DB954;
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 20px;
        }

        #song-list {
            max-width: 900px;
            margin: 0 auto;
        }

        .song {
            background-color: #181818;
            border-radius: 8px;
            margin-bottom: 20px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s ease-in-out;
        }

        .song:hover {
            transform: scale(1.03);
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #fff;
        }

        p {
            margin-bottom: 5px;
            color: #b3b3b3;
        }

        audio {
            width: 100%;
            margin-top: 10px;
            outline: none;
        }

        /* Styling the audio controls */
        audio::-webkit-media-controls-panel {
            background-color: #181818;
        }

        audio::-webkit-media-controls-play-button,
        audio::-webkit-media-controls-current-time-display,
        audio::-webkit-media-controls-time-remaining-display {
            color: #fff;
        }

        /* Scrollbar styling */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-thumb {
            background-color: #1DB954;
            border-radius: 10px;
        }

        ::-webkit-scrollbar-track {
            background-color: #282828;
        }

        button:hover {
            transform: scale(1.05);
            background-color: #1ed760;
        }
    </style>
    <script>
        function handleAudioPlay(event) {
            const allAudios = document.querySelectorAll('audio');
            allAudios.forEach(audio => {
                if (audio !== event.target) {
                    audio.pause();
                }
            });
        }
    </script>
</head>

<body>
    <h1>SpotiNode</h1>
    <div id="song-list">
        <% songs.forEach(song=> { %>
            <div class="song">
                <h2>
                    <%= song.title %> by <%= song.artist %>
                </h2>
                <p>Album: <%= song.album || 'N/A' %></p>
                <p>Genre: <%= song.genre || 'N/A' %></p>
                <audio controls onplay="handleAudioPlay(event)">
                    <source src="<%= song.file_path %>" type="audio/mpeg">
                    Your browser does not support the audio element.
                </audio>
            </div>
        <% }); %>
    </div>
    <div style="text-align: center; margin-bottom: 20px;">
        <a href="/insert-music" style="text-decoration: none;">
            <button style="background-color: #1DB954; color: #fff; border: none; padding: 10px 20px; font-size: 1rem; cursor: pointer; border-radius: 8px; transition: transform 0.2s ease-in-out, background-color 0.3s ease;">
                Add New Music
            </button>
        </a>
    </div>
</body>

</html>
